## ip协议

```
唯一标识一个网络设备，IP 地址 = {<网络号>，<主机号>}。
- 网络号：它标志主机所连接的网络地址表示属于互联网的哪一个网络。
- 主机号：它标志主机地址表示其属于该网络中的哪一台主机。
```



## IP 协议的作用是什么？

```
IP协议（Internet Protocol）是TCP/IP协议族的核心协议，主要负责在网络上进行数据包的寻址和路由。

IP 数据报中携带源 IP 地址和目的 IP 地址来表示该数据包的源主机和目标主机。
IP 数据报在传输过程中，每个中间节点（IP 网关、路由器）只根据网络地址来进行转发，
如果中间节点是路由器，则路由器会根据路由表选择合适的路径，决定转发到哪个网络或者下一跳路由器。
```

## IP 地址有哪些分类？

```
ip地址总共32位

A 类地址 (1~126)：以 0 开头，网络号占前 8 位，主机号占后面 24 位。
B 类地址 (128~191)：以 10 开头，网络号占前 16 位，主机号占后面 16 位。
C 类地址 (192~223)：以 110 开头，网络号占前 24 位，主机号占后面 8 位。
D 类地址 (224~239)：以 1110 开头，保留为多播地址。
E 类地址 (240~255)：以 1111 开头，保留位为将来使用
```



## PING

### PING 命令的作用是什么？

```
是一种常用的网络诊断工具，经常用来测试网络中主机之间的连通性和网络延迟
```

### PING 命令的工作原理是什么？

```
ICMP

TODO：https://javaguide.cn/cs-basics/network/other-network-questions.html#ping-命令的作用是什么
```

